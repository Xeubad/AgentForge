# Cursor legacy rules（已 deprecated，仅作兼容）

> ⚠️ 注意：`.cursorrules` 已被 Cursor 官方标为 **legacy**。新项目请使用 `.cursor/rules/*.mdc`。
> 如果当前项目已经有完整的 `.cursor/rules/` 目录，本文件中的规则会与 .mdc 规则**同时**注入到 system prompt，造成重复消耗 token 与规则冲突——建议直接**删除本文件**。
> 本文件仅在以下情况保留：
> 1. 当前项目尚未迁移到 `.cursor/rules/*.mdc`
> 2. 团队中有人使用极老版本 Cursor 不支持 `.mdc`
> 3. 你希望作为最小化兜底规则单独存在

## 默认协作

- 默认使用中文，先给结论，再给必要理由。
- 仓库是唯一事实来源；未读取到的业务规则、接口字段和配置键不得臆造。
- 修改前先搜索并读取相关文件，优先复用已有组件、工具函数、类型、schema 和配置。
- 涉及外部库、框架能力或第三方 API 时先查官方文档，不猜用法。
- 需求缺少关键边界、数据结构、权限规则或错误处理策略时，先提问再编码。

## 执行纪律

- 安全性 > 正确性 > 验证闭环 > 执行效率 > 表达风格。
- 一次只推进一个明确目标，采用最小改动原则。
- 不顺手重构无关代码，不破坏既有架构分层。
- 禁止硬编码密钥、token、密码、生产地址或隐私数据。
- 禁止吞掉异常；所有可失败路径必须有错误处理或明确向上抛出策略。

## 代码质量

- 保持项目现有格式、缩进、导入顺序和命名风格。
- 命名必须语义化；禁止 `a`、`b`、`tmp` 等无意义名称。
- 复杂逻辑拆分为小函数；不要为了“更短”牺牲可读性。
- 只在代码意图不容易直接读出时添加简短注释。

## 验证与收尾

- 没有验证，不算完成。
- 能运行测试、类型检查、lint、build 或启动检查就运行。
- 不能验证时，必须说明未验证项、原因和风险。
- 最终回复包含：改动摘要、验证命令和结果、剩余风险。
- 完成结论使用 `Accept / Revise / Block`；证据不足不能给 `Accept`。

## 前端补充

- UI 功能优先，风格克制，参考 Notion / Linear。
- 默认纯色背景、扁平化设计；避免渐变、光泽、纹理和装饰性光斑。
- 所有交互元素必须有可见文本或 `aria-label`。
- 文本不得溢出容器；移动端和桌面端都要检查主要布局。

