---
description: Cursor 项目通用工作流、安全边界和上下文规则
alwaysApply: true
---

# Cursor 通用工作流

## 默认行为

- 默认使用中文说明，先给结论，再给必要理由。
- 仓库是唯一事实来源；仓库里没有的背景、接口、业务规则和约束不得臆造。
- 不确定就明确说不确定，并说明需要读取哪些文件或文档。
- 不输出大段无关解释；重点说明改了什么、为什么、如何验证、剩余风险。

## 开工流程

1. 先读取项目入口规则、README、任务状态、相关源码和测试。
2. 非琐碎任务先形成冲刺合同：目标、范围、排除项、验收标准、验证方式、主要风险。
3. 同一时间只推进一个明确目标；已有进行中任务时先延续或显式调整。
4. 修改前必须搜索现有实现，优先复用已有组件、工具函数、类型、schema、API client 和配置。
5. 涉及外部库、框架能力、Cursor 配置或第三方 API 时，先查官方文档，不凭记忆猜。

## 执行纪律

- 安全性 > 正确性 > 验证闭环 > 执行效率 > 表达风格。
- 采用最小改动原则，不顺手重构无关代码。
- 不破坏既有分层、命名、错误处理、日志和测试风格。
- 禁止硬编码密钥、token、密码、生产地址或隐私数据。
- 禁止吞掉异常；所有可失败路径必须有错误处理或向上抛出策略。
- 禁止在没有依据时新增业务字段、状态枚举、错误码或配置键。

## 代码生成约束

- 直接修改项目文件，避免只给片段，除非用户明确要求讲解。
- 保持现有格式、缩进、导入顺序和命名风格。
- 变量、函数、类型命名必须语义化；禁止 `a`、`b`、`tmp` 这类无意义名称。
- 复杂逻辑优先拆分为小函数；不要为了“更短”牺牲可读性。
- 仅在代码意图不容易直接读出时添加简短注释。

## 用户澄清条件

遇到以下情况先暂停并提问：

- 需求缺少关键业务边界、数据结构、权限规则或错误处理策略。
- 多种实现会显著影响架构、数据兼容性、性能或安全。
- 需要删除数据、迁移结构、重写大文件或修改公共接口。

