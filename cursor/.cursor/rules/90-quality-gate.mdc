---
description: 完成定义、验证闭环、交接和评估规则
alwaysApply: true
---

# 质量门禁

## 完成定义

没有验证，不算完成。完成声明必须同时满足：

- 实现符合本轮目标和明确排除项。
- 已运行可用的验证命令，或明确说明无法验证的原因。
- 系统级用户路径没有被明显破坏。

## 推荐验证顺序

1. 最小相关测试：只跑与改动直接相关的测试。
2. 类型检查：TypeScript、schema、配置文件语法检查。
3. Lint/format：只在项目已有脚本时运行。
4. 构建或启动检查：影响运行路径时执行。
5. 手动路径：UI、接口或关键业务流程需要时补充。

## 收尾输出

最终回复必须包含：

- 改动摘要。
- 运行过的验证命令和结果。
- 未验证内容、原因和风险。
- 下一步最佳动作，仅在确实有价值时给出。

## 评估结论

使用以下结论之一：

- `Accept`：实现、验证、系统行为都满足。
- `Revise`：方向正确，但仍有未完成项、边界缺口或验证不足。
- `Block`：需求不清、基础状态失败、关键依赖未知或风险不可控。

证据不足时禁止给 `Accept`。

