---
description: React、TypeScript 和前端 UI 开发规则
globs:
  - "**/*.tsx"
  - "**/*.jsx"
  - "**/*.ts"
  - "**/*.js"
  - "**/*.css"
  - "**/*.scss"
  - "**/*.mdx"
alwaysApply: false
---

# 前端开发规则

## React / TypeScript

- 优先使用函数组件和 Hooks，不新增 Class 组件。
- Props、API 响应、表单数据和复杂状态必须显式建模，禁止无理由使用 `any`。
- 异步状态必须覆盖 `loading / error / data`，避免只处理成功路径。
- 组件拆分以职责为边界：数据获取、状态协调、纯展示不要混成难维护的大组件。
- 优先复用项目已有 UI 组件、hooks、schema、样式 token 和工具函数。

## UI 设计

- 功能优先美学，界面服务于实际操作效率。
- 风格参考 Notion / Linear：克制、清晰、密度适中、信息层级明确。
- 默认使用纯色背景和扁平化设计；避免渐变、光泽、纹理、装饰性光斑。
- 阴影只用于必要层级区分，优先用间距、边框、字号和颜色建立层级。
- 不做营销式落地页，除非用户明确要求；应用应把主要可用界面放在第一屏。

## 可访问性与交互

- 按钮、链接、输入框等交互元素必须有可见文本或 `aria-label`。
- 图片必须有合理 `alt`；装饰图使用空 `alt`。
- 表单必须展示校验错误，并保留用户已输入内容。
- 文本不得溢出容器；移动端和桌面端都要检查主要布局。

## 性能

- 大列表使用分页、虚拟滚动或增量加载，避免一次性渲染大量 DOM。
- 避免在 render 中做重计算；必要时使用 memoization，但不滥用。
- 图片、字体和第三方脚本按框架最佳实践加载。

## 验证

- 修改交互或布局后，至少运行项目已有的 lint/typecheck/test/build 中可用项。
- 视觉改动需要说明检查过的视口或手动验证路径。
